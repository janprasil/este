/* @flow */
import React from 'react';

const style = {
  width: '100%',
  float: 'left',
  borderTop: '1px solid black',
};

const AppFooter = () => (
  <div style={style}>
    <p>Loan Calculator</p>
  </div>
);

export default AppFooter;
